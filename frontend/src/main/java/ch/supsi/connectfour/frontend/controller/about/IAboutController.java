package ch.supsi.connectfour.frontend.controller.about;

/**
 * Defines the behaviour that a generic about controller should expose to the dispatcher
 */
public interface IAboutController {
    void manageAbout();
}